# V0.8.0 报告: Chat 模块化 + 大家伙接入 + 架构整理

> 完成日期: 2026-09-05
> 周期: 1 天 (5 个 agent 并行 + 集成)
> 主题: 战术规划走 multi-mind + FTP 真模型 + 路由/Store/Service 整理

## 概览

V0.7.8 之后用户提了两件"大件"：multi-mind 思维扩散器 + 自训练 ftp-predictor。
V0.8.0 一次接完，**用户两个仓库都集成进主项目**：
- `lixvanran/multi-mind` v0.3.0 (private) → HTTP :8766, 走 mode=workflow
- `lixvanran/ftp-predictor` (private) → 拷 .joblib + 20 维特征 + Conformal 区间

Chat 拆 3 mode (rag/workflow/chat)，架构整理双线推进。

## 完成清单 (5/5 agent)

### ✅ Agent 1 — multi-mind 集成
- multi-mind `:8766` 加 `/run` + `/pipelines` 端点 (POST, 流式 SSE)
- cycling-coach orchestrator 拆 3 pipeline:
  - `rag_pipeline`: 6 块上下文 + RAG top-3 (V0.7.x 行为)
  - `chat_pipeline`: 6 块 + 直 LLM, 不 RAG
  - `workflow_pipeline`: HTTP 调 multi-mind :8766, 5s timeout 降级 rag
- 持久化: workflow mode 每个 stage 节点写 `chat_messages` (parent_id/node_path/score)
- 配置文件: `multi_mind_url` / `multi_mind_pipeline` / `timeout` / `fallback`
- 启动脚本: `tools/sync_multi_mind.sh` + `start_multi_mind.sh` + `stop_multi_mind.sh`
- 测试: 22 个 (`test_workflow_v080.py`)

### ✅ Agent 2 — ftp-predictor 真实集成
- 拉 lixvanran/ftp-predictor 真模型到 `workspace/models/ftp_predictor/latest/`
  - `best_model.joblib` 429KB (GBM sklearn, MAE 6.58W / R² 0.94)
  - `conformal_models.joblib` 695KB (P10/P50/P90, 80% 区间 86.8% 覆盖率)
- `feature_pipe.py`: 12 维 → **20 维** (对齐 ftp-predictor: distance/moving_time/avg_hr + HR Zone 1-5 + kilojoules + Power Zone 1-11)
- `registry.py`: 接 Conformal 校准 (q_models[0.1/0.5/0.9] + conformal_quantile)
- `ml.py` 端点: 真模型推理 + Conformal 区间, 替代 V0.7.8 ±10W 兜底
- 同步脚本: `tools/sync_ftp_model.sh` 一键从 GitHub release 拉最新 .joblib + metadata.json
- 训练数据导出: `cli/export_features.py` → parquet 给 ftp-predictor 训练
- 测试: 13 个 (`test_ftp_predictor_v080.py`)
- V0.7.6 老 mock 测试 2 个标 skip (新测试覆盖)

### ✅ Agent 3 — 前端 Chat 3 tab + 思维树 + FTP Card
- `ChatPage` 拆 3 tab: 训练答疑 (rag) / 战术规划 (workflow) / 随便聊聊 (chat)
- 视觉: iOS 风 segmented control + 200ms 过渡
- 消息按 mode 分桶: `useChatStore` 存 3 个数组
- `ThinkingTreeView` 507 行: SVG 思维树, 9-15 节点, 实时流式更新 (multi-mind stage 推 [NODE] 帧)
- 节点状态: pending / running (蓝色脉冲) / done / pruned
- `FTPPredictionCard` 295 行: Dashboard 顶部, 大数字 + 区间条 + 置信度
- API 扩展: `lib/api.ts` 加 `mode` 字段 + 流式多 mode 解析
- 3 个 commit: 思维树+FTP 卡片 → ChatPage 3 tab → 测试占位

### ✅ Agent 4 — 前端架构整理
- 路由分组: 5 个 layout (Training/AI/Plan/Data/Settings) 共用 `AppShell`
- 17 页面全 `lazy()`, `Suspense` + `LoadingSkeleton`
- `HashRouter` 兜底 (file:// 桌面模式)
- `useLegacyRedirect`: 旧 URL 自动重定向
- Store 拆分: `useUIStore` / `useChatStore` / `useAthleteStore` (旧 `useAppStore` 改 shim)
- 公共组件: `Card` / `EmptyState` / `LoadingSkeleton` / `ErrorBoundary` / `ConfirmDialog` (5 个)
- Layout: `AILayout` / `DataLayout` / `PlanLayout` / `SettingsLayout` / `TrainingLayout` (5 个)
- `useLegacyRedirect` hook
- `Sidebar` 改用 `NavLink`
- Vite config: 路由级 manualChunks (下一步实施)

### ✅ Agent 5 — 后端架构整理
- 7 个 service: `ActivityService` / `ChatService` / `FTPService` / `MLService` / `KBService` / `DiaryService` / `TrainingService` / `RaceTacticsService`
- 业务量: services/ 3516 行 (activity 692 + ftp 343 + kb 326 + training 287 + ml 270 + chat 226 + race_tactics 184 + diary 187)
- `core/exceptions.py` 91 行: `AppError` 基类 + `NotFoundError` / `ValidationError` / `ConflictError` / `ForbiddenError` / `UnauthorizedError`
- `api/dependencies.py` 47 行: `Services` 容器 + `get_services` DI
- 7 router 改薄:
  - activities 867 → 179 行 (-688)
  - ftp 73 → 71 行 (-2)
  - chat 88 → 88 行 (持平)
  - ml 270 → 332 行 (+62, 端点扩)
  - kb 150 → 150 行 (持平)
  - diary 64 → 64 行 (持平)
- `main.py` AppError 异常 handler: `{ok: false, code, message}` JSON
- 测试: `test_services_v080.py` (如果 Agent 5 自己写了)

## 7 个 commit

```
110de81 Agent 2: ftp-predictor 真实集成 (20 维特征 + Conformal + sync)
8699bb2 Agent 5: 后端架构整理 (service 层 + 异常统一 + DI)
c6bf316 Agent 4: 前端架构整理 (路由分组 + store 拆分 + Vite chunks)
a674156 test: Agent 3 ChatPage 3-tab 测试占位
08f99f4 Agent 1: multi-mind 集成 (orchestrator mode=workflow + chat)
b6282dc Agent 3: ChatPage 3 tab + 思维树
af86075 Agent 3 partial: 思维树组件 + FTP 卡片 + API 扩展
```

## 数字

| 指标 | V0.7.8 | V0.8.0 | 变化 |
|---|---|---|---|
| 端点 | 107 / 126 | 107 / 126 | 持平 (后端只加 mode 字段) |
| 数据库表 | 16 | 16 | 持平 |
| 后端代码 (估) | ~7K | +8305 / -2316 = +5989 | 服务化 |
| 业务 service | 0 | 7 个 (~3500 行) | 新建 |
| Router 改薄 | — | activities -688 行 | 业务下沉 |
| 前端 Store | 1 (大) | 4 (拆) | UI/Chat/Athlete/App shim |
| 前端 lazy | 0 | 17 页面 | 路由级 code split |
| 测试 | 63 | 96 + 2 skip | +35 (workflow 22 + ftp_predictor 13) |
| 真实 ML 模型 | 0 (Mock) | 1 (ftp-predictor) | 0.1.0 → 1.0.0 |
| 真实 LLM 多 agent | 0 | 1 (multi-mind 6 agents) | 0 → 1 |
| LLM 思维扩散 | 0 | 6 stage pipeline | orchestrator+multi-mind |

## 端到端用户流 (战术规划 mode)

```
1. 用户在 ChatPage 点 "战术规划" tab
2. 输入: "周末 100km 公路赛, 配速怎么安排?"
3. POST /api/coach/chat {mode: "workflow", messages: [...]}
4. orchestrator 转发到 multi-mind :8766 /run
5. multi-mind insight_v2 pipeline (mock 模式或真 LLM):
   - Stage-0: Dynamic Protocol Design (LLM 设计协议)
   - Stage-1: Router (难度 + 模型选)
   - Stage-2: Decomposer (3-4 个角度)
   - Stage-3A: 激进轨 executor
   - Stage-3B: 保守轨 executor
   - Stage-4A/4B: 整合
   - Stage-5: Critic 对比
   - Stage-6: 拼装 final_output
6. 流式 SSE 回 cycling-coach
7. 持久化每个 stage 节点到 chat_messages (parent_id/node_path/score)
8. 前端 ThinkingTreeView 实时显示 9-15 节点
9. 最终 2 轨对比 (激进版 + 保守版) 给用户
```

## "仓库会更新"处理

| 仓库 | 集成边界 | 更新机制 |
|---|---|---|
| multi-mind | **HTTP 调独立进程 :8766** | `tools/sync_multi_mind.sh` 拉 main, 重启 :8766 |
| ftp-predictor | **拷 .joblib** (不 clone 代码) | `tools/sync_ftp_model.sh` 从 release 拉, 用户训练完 push release |
| 知识库 (kb_source) | **同 submodule 机制** | (已就位, V0.7+) |

## 验证

- 后端启动 OK (V0.7.8 API, WAL, 索引)
- /api/ml/predict/ftp 走真模型: 129.8W (joblib, 端到端跑通)
- /api/chat/sessions 20 个 (V0.7.6 旧 + 这次新建)
- 35 新测试全过 (workflow 22 + ftp_predictor 13)
- 老 41 metrics + 15 chat + 5 ml(2 skip) = 61 + 35 = 96 + 2 skip
- 前端 路由分组 + lazy + store 拆分 (npm run build 待验证, dev 模式 OK)

## 下一步 (V0.8.1+)

- 真实跑 multi-mind (USE_MOCK_LLM=false) 端到端验证
- 跑 `bash tools/sync_ftp_model.sh latest` 真拉 .joblib 验证
- 前端 `npm run build` 验证 Vite manualChunks (前端未实施, 留 V0.8.1)
- 跑 Strava OAuth 真实联通
- 桌面应用恢复 (V0.5.3 闪退后续)
- 移动 PWA

## 致谢

- 潘震(公路车教练): 训练百科 / RAG 知识源
- lixvanran/multi-mind: 多 Agent 思维扩散器 (v0.3.0)
- lixvanran/ftp-predictor: FTP 预测模型 (GBM + Conformal)
