# V0.7.5.4 DEV-18: 加 ~= 上限 (兼容到下个主版本)
# 同步自 pyproject.toml [project.dependencies]
# 跟 pyproject.toml 保持一致 (pip install -r requirements.txt 等价)
# 兼容老用户,内容同步自 pyproject.toml [project.dependencies]
# 实际项目用 pyproject.toml

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.6,<3.0
pydantic-settings>=2.2
openai>=1.12,<2.0
httpx>=0.27,<1.0
python-multipart>=0.0.9
aiofiles>=23.2
sqlalchemy>=2.0,<3.0
fitparse>=1.2
numpy>=1.26,<3.0
pandas>=2.1
scipy>=1.11
reportlab>=4.0  # V0.7.3 PDF 周报
fit-tool>=0.9.16  # V0.7.4 FIT 课程导出

# 测试
pytest>=8.0
pytest-asyncio>=0.23

# V0.7.3: PDF 周报
reportlab>=4.0
# V0.7.4: FIT 课程导出
fit-tool>=0.9.16
